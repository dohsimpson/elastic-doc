// Generated by CoffeeScript 2.0.3
(function() {
  var app;

  app = {
    initialize: function() {
      return document.addEventListener('deviceready', this.onDeviceReady.bind(this), false);
    },
    onDeviceReady: function() {
      return this.receivedEvent('deviceready');
    },
    receivedEvent: function(id) {
      var root;
      // parentElement = document.getElementById id
      // listeningElement = parentElement.querySelector '.listening'
      // receivedEvent = parentElement.querySelector '.received'

      // listeningElement.setAttribute 'style', 'display:none;'
      // receivedElement.setAttribute 'style', 'display:block;'
      root = document.getElementById(id);
      cordova.InAppBrowser.open('res/elastic_doc/index.html', '_self', 'location=no');
      return console.log('Received Event: ' + id);
    }
  };

  app.initialize();

}).call(this);
